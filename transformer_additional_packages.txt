Do not forget to activate your virtualenv, if you have any before running these commands
Open the linux terminal and enter these commands one by one
git clone https://github.com/Tony-Y/pytorch_warmup.git
cd pytorch_warmup/
python setup.py install (if in virtualenv)
python3 setup.py install (if in system env)
sudo python3 setup.py install
pip install torch-summary
pip install einops
pip install 'git+https://github.com/katsura-jp/pytorch-cosine-annealing-with-warmup'
pip install matplotlib
